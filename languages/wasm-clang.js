export async function runCOrCpp(_code, language) {
  throw new Error(`${language.toUpperCase()} WASM compiler integration is scaffolded but not yet wired. Add a browser Clang/LLD WASM toolchain.`);
}
